<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/head') %>
</head>
    <body >
        <header>
            <%- include('../partials/header') %>
        </header>
        <%- include('../partials/navbar') %>
        <main style="margin-left: 200px; margin-top: 80px; padding: 20px; overflow-wrap: anywhere;" class="flex flex-col items-center text-2xl">
            <h1 class="text-4xl font-extrabold leading-loose px-4">
            Employees</h1>
            <% if (result.errorMessage!=null){ %>
            <br>
            <div role="alert">
            &nbsp;<div class="text-2xl bg-red-500 text-white
            font-bold rounded-t px-4 py-2">
            Error - <%= result.errorMessage %>
            </div>&nbsp;
            </div>
            <% } %>
            <% if(result.employees !=null){ %>
            <div>
            <%- include('../partials/allEmployees') %>
            </div>
            <% } %>
        </main>
        <footer>
            <%- include('../partials/footer') %>
        </footer>
    </body>
</html>